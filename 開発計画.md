# 開発計画: 育児記録エージェント（koto-log）

| 項目 | 内容 |
|---|---|
| 作成日 | 2026-06-18 |
| 最終更新 | 2026-06-20 |
| 目的 | Design Doc を Claude Code での実行手順に落とし込む |
| 進め方 | 1セッション=1タスク。各タスクは「完了条件（テスト/手動確認）」を満たしたら次へ |

---

## 0. 基本方針

- **CLI で完成させてから LINE に載せる。** P1 までで「自然言語→保存→確認文」がローカルで完結する状態を作る。
- **テスト駆動で進める。** DB・時刻正規化・ツール選択は壊れやすい中核。先にテストを書いてから実装する。
- **LLM 依存部分は薄く保つ。** ツール定義（JSONスキーマ）と executor はモデル非依存。LiteLLM の `model` 文字列だけで local⇄Claude を切替。
- **過剰設計しない。** ルータ・マルチエージェント・複数子対応は初期スコープ外。

---

## 1. 技術スタック

| レイヤー | ローカル開発 | 本番 |
|---|---|---|
| 言語/パッケージ | Python 3.13 / uv | 同左 |
| LLM | Ollama (Qwen2.5:7b) | Claude Haiku 4.5 (`anthropic/claude-haiku-4-5-20251001`) |
| LLM 抽象化 | LiteLLM | 同左 |
| DB | SQLite (kotolog.db) | Turso / libSQL |
| Web | FastAPI + uvicorn | 同左 (Docker on Render) |
| テスト | pytest + pytest-cov | — |
| フロント | — | Jinja2 + Chart.js |

---

## 2. ディレクトリ構成（現状）

```
koto-log/
├── pyproject.toml
├── render.yaml             # Render デプロイ設定
├── Dockerfile
├── .env.example
├── src/kotolog/
│   ├── types.py            # RecordType / FeedingSubType / DiaperSubType enum
│   ├── config.py
│   ├── db/                 # schema.sql / connection.py / crud.py
│   ├── utils/              # timeparse.py / subtype.py
│   ├── tools/              # definitions.py / executor.py
│   ├── llm/client.py
│   ├── agent/loop.py
│   ├── templates/
│   │   └── dashboard.html
│   ├── line/
│   │   ├── webhook.py
│   │   ├── dashboard.py
│   │   └── reply.py
│   └── cli.py
├── evals/
└── tests/
    ├── unit/               # FakeLLM パターンで LLM 非依存
    ├── integration/        # 実 DB / Turso
    └── e2e/
```

---

## 3. 実行計画（フェーズ別）

### P0: Setup ✅
| ID | 内容 | 状態 |
|---|---|---|
| T0.1 | uv init・ディレクトリ構成・依存追加 | ✅ |
| T0.2 | config.py: .env 読込 | ✅ |
| T0.3 | Ollama 起動・LiteLLM 経由で1応答 | ✅ |

### P1: Core（CLI）✅
| ID | 内容 | 状態 |
|---|---|---|
| T1.1 | DB 層: スキーマ・接続・CRUD | ✅ |
| T1.2 | 時刻正規化 | ✅ |
| T1.3 | ツール3関数のスキーマ + executor | ✅ |
| T1.4 | LiteLLM ラッパ | ✅ |
| T1.5 | エージェントループ | ✅ |
| T1.6 | 対話 CLI | ✅ |
| T1.7 | ツール選択の評価テスト | ✅ |

### P1.5: MVP+ ✅
| ID | 内容 | 状態 |
|---|---|---|
| T1.8 | query_records の集計化（by_type / by_sub_type） | ✅ |
| T1.9 | sub_type 正規化（同義語→正規形） | ✅ |
| T1.10 | 1日/週まとめ | ✅ |
| T1.11 | 前回いつ / 経過時間（period=latest） | ✅ |

### P2: LINE 連携 ✅
| ID | 内容 | 状態 |
|---|---|---|
| T2.1 | FastAPI Webhook + 署名検証 + 即 ACK | ✅ |
| T2.2 | message.id 冪等化 | ✅ |
| T2.3 | Reply API 連携 | ✅ |
| T2.4 | ngrok 実機確認 | ✅ |

### P3: デプロイ ✅
| ID | 内容 | 状態 |
|---|---|---|
| T3.1 | Dockerfile（uv 公式イメージ） | ✅ |
| T3.2 | Turso / libSQL 対応（_LibsqlConn ラッパー） | ✅ |
| T3.3 | Render デプロイ（render.yaml + 環境変数） | ✅ |
| T3.4 | Claude Haiku 切替（`anthropic/claude-haiku-4-5-20251001`） | ✅ |

### P4: 機能拡張（継続中）
| ID | 内容 | 状態 |
|---|---|---|
| T4.1 | LINE 返答スタイル最適化（常態・短文・マークダウンなし） | ✅ |
| T4.2 | 操作一覧ショートカット（LLM バイパス） | ✅ |
| T4.3 | RecordType/SubType enum で型定義を一元管理 | ✅ |
| T4.4 | ダッシュボード（授乳タイムライン・7日間サマリ） | ✅ |
| T4.5 | テストカバレッジ計測（pytest-cov） | ✅ |
| T4.6 | agent/loop.py の FakeLLM 単体テスト | ✅ |
| T4.7 | リッチメニュー設定 | 未着手 |
| T4.8 | Push 通知（リマインダー） | 未着手 |
| T4.9 | ダッシュボード拡充（睡眠グラフ・おむつ推移） | 未着手 |

---

## 4. マイルストーン

| MS | 状態 |
|---|---|
| M1: CLI で記録・集計・修正 | ✅ 完了 |
| M1.5: 集計・まとめ・前回経過 | ✅ 完了 |
| M2: LINE 実機で往復 | ✅ 完了 |
| M3: 公開 URL + Claude で毎日使える | ✅ 完了 (`koto-log.onrender.com`) |
| M4: ダッシュボードでデータを見る | ✅ 実装済み (`/dashboard`) |
